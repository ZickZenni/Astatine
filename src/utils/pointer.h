#pragma once

#include <string>

/**
 * Converts a pointer to its hexadecimal string representation.
 *
 * @param ptr A pointer to be converted into a hexadecimal string.
 * @return A string representing the hexadecimal representation of the given pointer.
 */
std::string ptrthex(void* ptr);